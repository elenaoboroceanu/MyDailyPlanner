@model DailyPlanner.DomainClasses.FlashcardType

@{
    ViewBag.Title = "Delete";
}

@using (Html.BeginForm("Delete", "Flashcard", FormMethod.Post, new { @class = "form-horizontal" }))
{
    <fieldset>
        <legend>Delete flashcard type</legend>

        @Html.Partial("_DeleteOrDetails", @Model)

        <div class="form-group">
            <div class="col-xs-5"></div>
            <div class=" col-xs-2 text-center">
                <input type="submit" class="btn btn-primary btn-block" value="Delete" />
            </div>
        </div>

    </fieldset>


}

<p>

    @Html.ActionLink("Back to List", "Index")
</p>
